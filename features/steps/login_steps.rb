Given /^user "([^"]*)" accesses the application$/ do |arg1|
  pending # express the regexp above with the code you wish you had
end

When /^the user signs in with Facebook$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^the user should see the name "([^"]*)"$/ do |arg1|
  pending # express the regexp above with the code you wish you had
end